# Find number of subarrays with even sum
# Given an array, find the number of subarrays whose sum is even.
# eg: [1, 2, 2, 3, 4, 1] -> outputs 9
#Sub arrays and not subset
#continuous elements only
#brute force is O(n)^2


def findSubArrays(inList):
    #input is a list
    #output is a number
    #number of sub arrays that add up to an even number
